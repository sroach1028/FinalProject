import { JobsActivePipe } from './jobs-active.pipe';

describe('JobsActivePipe', () => {
  it('create an instance', () => {
    const pipe = new JobsActivePipe();
    expect(pipe).toBeTruthy();
  });
});
